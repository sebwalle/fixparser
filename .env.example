# =====================================================
# FIX Analyzer Environment Variables
# =====================================================
# Copy this file to .env.local and configure as needed
# =====================================================

# -----------------------------------------------------
# API Authentication (Optional)
# -----------------------------------------------------
# Bearer token for API authentication
# If set, requests to /api/fix/parse and /api/messages/ingest
# must include: Authorization: Bearer <token>
# Leave empty to disable authentication
FIX_API_TOKEN=

# -----------------------------------------------------
# CORS Configuration
# -----------------------------------------------------
# Comma-separated list of allowed origins
# Examples:
#   Development: *
#   Production: https://yourapp.com,https://api.yourapp.com
# Default: * (allow all origins)
ALLOWED_ORIGINS=*

# -----------------------------------------------------
# AI Repair Feature (Optional, Disabled by Default)
# -----------------------------------------------------
# Feature flag for AI-powered message repair
# WARNING: Enabling this sends FIX message content to external AI providers
# Do not enable if messages contain sensitive or regulated information
# Set to 'true' to enable, requires FIX_AI_PROVIDER and FIX_AI_KEY
ENABLE_AI_REPAIR=false

# AI provider name (e.g., 'openai')
# Required if ENABLE_AI_REPAIR=true
FIX_AI_PROVIDER=

# API key for AI provider
# Required if ENABLE_AI_REPAIR=true
FIX_AI_KEY=

# -----------------------------------------------------
# Storage Configuration
# -----------------------------------------------------
# Toggle between in-memory and Vercel KV storage
# Set to 'true' to use Vercel KV (requires KV environment variables below)
# Set to 'false' (or leave empty) to use in-memory storage (default)
USE_KV=false

# -----------------------------------------------------
# Vercel KV Configuration (Optional)
# -----------------------------------------------------
# Only required if USE_KV=true
# These are automatically populated when linking a Vercel KV database
# to your project in the Vercel Dashboard

# Vercel KV connection URL
KV_URL=

# Vercel KV REST API URL
KV_REST_API_URL=

# Vercel KV REST API token (write access)
KV_REST_API_TOKEN=

# Vercel KV REST API read-only token
KV_REST_API_READ_ONLY_TOKEN=

# =====================================================
# Notes:
# =====================================================
# - All variables are optional with sensible defaults
# - For development, only ALLOWED_ORIGINS=* is typically needed
# - For production, set FIX_API_TOKEN and specific ALLOWED_ORIGINS
# - AI repair feature is disabled by default for privacy
# - In-memory storage is sufficient for most use cases
# - Use Vercel KV for persistent storage across deployments
# =====================================================
